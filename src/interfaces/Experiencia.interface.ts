// Referencia a habilidadas blandas, trabajo en equipo, buen liderazgo, etc

export interface IExperiencia {
    id: number;
    descripcion: string;
    desde_fecha: string;
    empresa: string;
    hasta_fecha: string;
    titulo: string;
}