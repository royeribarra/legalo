// Referencia a habilidadas blandas, trabajo en equipo, buen liderazgo, etc

export interface IEstudio {
    id: number;
    descripcion: string;
    desde_fecha: string;
    institucion: string;
    hasta_fecha: string;
    titulo: string;
    ubicacion: string;
}